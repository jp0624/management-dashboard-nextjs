export const DIVIDER = `/`
export const ROOT = `.${DIVIDER}`

export const API_PATH = `${ROOT}api`
export const DB_PATH = `${ROOT}public${DIVIDER}data`

export const TARGETS_FOLDER = `${DIVIDER}targets`
export const TARGETS_JSON_FILE = `${DIVIDER}targets.json`
export const TARGETS_JSON_PATH = `${DB_PATH}${TARGETS_JSON_FILE}`

export const HISTORY_FOLDER = `${DIVIDER}history`
export const HISTORY_JSON_FILE = `${DIVIDER}targets-history.json`
export const HISTORY_JSON_PATH = `${DB_PATH}${HISTORY_JSON_FILE}`
